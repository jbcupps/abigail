# UAT Test Keyset — copy to uat-keys.env and fill in real values.
# This file is gitignored. Never commit the filled-in copy.

# --- Provider API key (at least one required) ---
OPENAI_API_KEY=

# --- IMAP Bridge credentials ---
UAT_IMAP_HOST=127.0.0.1
UAT_IMAP_PORT=7654
UAT_IMAP_USER=heph.prometheus@pm.me
UAT_IMAP_PASSWORD=
UAT_IMAP_SECURITY=STARTTLS
